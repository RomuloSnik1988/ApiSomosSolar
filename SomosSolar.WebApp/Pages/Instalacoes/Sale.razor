@page "/addEquipamentos/{id}"
@inherits SaleInstalacaoPage

<MudText Typo="Typo.h6">Adicione os equipamentos da instalação</MudText>
@if (IsBusy)
{
    <MudPaper Class="pa-8 mt-4">
        <MudSkeleton Height="55px" />
        <MudSkeleton Height="110" />
        <MudSkeleton Height="55px" />
        <MudSkeleton Height="110" />
        <MudSkeleton Height="55px" />
    </MudPaper>
}
else
{
    <MudPaper Class="pa-8 mt-4">
        <EditForm Model="@InputModelInstalacao">
            <DataAnnotationsValidator />

            <MudTextField T="int" Label="Cliente"
                          @bind-Value="InputModelInstalacao.ClienteId"/>

            <MudTextField T="DateTime" Label="Data da Instalação"
                          @bind-Value="InputModelInstalacao.DataInstalacao" />

           
            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary">Salvar</MudButton>
            <MudButton Href="/instalacoes" Variant="Variant.Filled" Color="Color.Primary">Voltar</MudButton>

        </EditForm>
    </MudPaper>

}

@* <MudText Typo="Typo.h6">Adicione os equipamentos desta instalação</MudText>

<MudDataGrid Items="@Instalacoes" Class="mt-4">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Instalação:</MudText>
        <MudSpacer />
       
    </ToolBarContent>
    <Columns>
        <PropertyColumn Property="x => x.Cliente" Title="Cliente"></PropertyColumn>
        <PropertyColumn Property="x => x.DataInstalacao" Title="Data de Instalação"></PropertyColumn>
        <TemplateColumn Class="d-flex justify-end">
            <CellTemplate>
            </CellTemplate>
        </TemplateColumn>
    </Columns>

</MudDataGrid> *@